<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="icon" type="image/x-icon" href="../../favicon.ico">
    <link rel="stylesheet" href="../../style.css">
    <title><%= title %></title>
</head>
<body>
<header>
    <nav>
        <h4>
            <svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 -960 960 960" width="24px" fill="#5f6368">
                <path d="M200-200h360v-200h200v-360H200v560Zm0 80q-33 0-56.5-23.5T120-200v-560q0-33 23.5-56.5T200-840h560q33 0 56.5 23.5T840-760v400L600-120H200Zm80-280v-80h200v80H280Zm0-160v-80h400v80H280Zm-80 360v-560 560Z"/>
            </svg>
            Message Board
        </h4>
        <!--TODO: if logged in - Welcome message to user and change login to logout -->
        <ul>
            <% if(currentUser) { %>
                <li class="cto"><a href="/messages/new">Create New Message</a></li>
            <% } %>
            <li><a href="/">Home</a></li>
            <% if(currentUser) { %>
                <li><a href="/logout">Logout</a></li>
            <% }else { %>
                <li><a href="/login">Login</a></li>
            <% } %>
            <% if(currentUser) { %>
                <% if(currentUser.role !== 'member' && currentUser.role !== 'admin') { %>
                    <li><a href="/become-member">Become a member</a></li>
                <% } %>
            <% } %>
        </ul>
    </nav>
</header>

